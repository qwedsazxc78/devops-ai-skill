# Ingress Migration Plan — {{deadline}}

**Run ID**: `{{runId}}` &middot; **Generated**: {{createdAt}} &middot;
**Skill**: ingress-migration-advisor v{{skillVersion}}

{{overflowBanner}}

## Summary

| Metric | Value |
|---|---|
| Total services in scope | {{totalServices}} |
| direct-gateway | {{directGatewayCount}} services in {{directGatewayBatches}} batch(es) |
| two-step | {{twoStepCount}} services in {{twoStepBatches}} batch(es) |
| swap-only | {{swapOnlyCount}} services in {{swapOnlyBatches}} batch(es) |
| defer | {{deferCount}} services (critical-tier or score 14) |
| **Deadline** | **{{deadline}}** |
| Bake buffer | {{bakeBufferDays}} days |
| Final cutover by | {{finalCutoverDate}} ({{deadlineMarginDays}} days margin) |

## Schedule

```mermaid
gantt
    title Ingress Migration Schedule (week of)
    dateFormat YYYY-MM-DD
    axisFormat %m-%d

    section direct-gateway
{{ganttDirectGateway}}

    section two-step
{{ganttTwoStep}}

    section swap-only
{{ganttSwapOnly}}
```

## Per-service decision table

| Service | Source class | Tier | Score | Path | Rationale |
|---|---|---|---|---|---|
{{perServiceRows}}

## Batches

{{batchBlocks}}

## Deferred services

{{deferredBlocks}}

## Risk register

| Severity | Service | Issue | Mitigation |
|---|---|---|---|
{{riskRows}}

## Warnings

{{warningList}}

## Audit

| Field | Value |
|---|---|
| Operator | {{operator}} |
| Deadline (input) | {{deadline}} |
| Bake buffer days | {{bakeBufferDays}} |
| Batch size cap | {{batchSizeCap}} |
| `--target-path` override | {{targetPathOverride}} |
| `--strict-tier-map` | {{strictTierMap}} |
| Tier map path | `docs/ingress-tier-map.yaml` |
| Tier map services listed | {{tierMapServiceCount}} |
| Inventory mode | {{inventoryMode}} |
| Schema version | {{schemaVersion}} |
| Skill version | {{skillVersion}} |
| Run ID | {{runId}} |
| State path | `{{statePath}}` |

---

_Generated by the `ingress-migration-advisor` skill. Re-run for a fresh
snapshot when the repo or tier map changes. Commit this `plan.md` and its
sibling `state.yaml` together so reviewers can audit the inputs._
