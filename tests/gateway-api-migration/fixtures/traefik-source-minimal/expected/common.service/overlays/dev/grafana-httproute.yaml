apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
  - name: app-gateway
    namespace: gateway
  hostnames:
  - grafana.dev.example.com
  rules:
  - backendRefs:
    - name: grafana
      port: 3000
