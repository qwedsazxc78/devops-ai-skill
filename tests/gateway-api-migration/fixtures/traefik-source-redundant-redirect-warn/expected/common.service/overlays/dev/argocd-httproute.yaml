apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  parentRefs:
  - name: app-gateway
    namespace: gateway
  hostnames:
  - argocd.dev.awoo.org
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80
